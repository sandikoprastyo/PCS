import React from 'react';
import {View, StyleSheet} from 'react-native';

const Form = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Form;